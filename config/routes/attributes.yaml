# Attribute-based routing for controllers and kernel
controllers:
    resource:
        path: ../../src/Controller/
        namespace: Bareapi\Controller
    type: attribute

kernel:
    resource: Bareapi\Kernel
    type: attribute
